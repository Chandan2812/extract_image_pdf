import PDFObject from './PDFObject';
declare class PDFIndirectReference<T extends PDFObject = PDFObject> extends PDFObject {
    static forNumbers: (objectNumber: number, generationNumber: number) => PDFIndirectReference<PDFObject>;
    objectNumber: number;
    generationNumber: number;
    constructor(enforcer: string, objectNumber: number, generationNumber: number);
    clone: () => this;
    toString: () => string;
    bytesSize: () => number;
    copyBytesInto: (buffer: Uint8Array) => Uint8Array;
}
export default PDFIndirectReference;
